{
  "name": "@uzo/database",
  "version": "1.0.0",
  "scripts": {
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "node seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.5.2"
  },
  "devDependencies": {
    "prisma": "^5.5.2"
  }
}
